<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>Get All Sent Invitations</title>
    {% block stylesheets %}{% endblock %}
    <link rel="stylesheet" href="{{absolute_url(asset('css/bootstrap.min.css'))}}"/>
  </head>
  <body>
      <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <a class="navbar-brand" href="#"></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor01">
          <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
              <a class="nav-link" href="#">
                Invited <span class="sr-only">(current)</span>
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="{{ absolute_url(asset(''))}}app_dev.php/api/getall/{{app.request.attributes.get('_route_params').userId}}">Sent Invitation</a>
            </li>
          </ul>
          <form class="form-inline my-2 my-lg-0">
            <input class="form-control mr-sm-2" type="text" placeholder="Search"/>
              <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
            </form>
        </div>
      </nav>
    <div>
	<table class="table table-hover">
        <thead>
          <tr>
            <th scope="col">Invited</th>
            <th scope="col">Id</th>
            <th scope="col">Sender Name</th>
            <th scope="col">Invited Name</th>
            <th scope="col">Message</th>
            <th scope="col">Status</th>
          </tr>
        </thead>
        <tbody>
          {% for invitation in invitations %}
          <tr class="table-active">
            <th scope="row"></th>
            <td>{{invitation.id}}</td>
            <td>{{invitation.senderName}}</td>
            <td>{{invitation.invitedName}}</td>
            <td>{{invitation.message}}</td>
            <td>{{invitation.status}}</td>
            <td>
              <a href="" class="btn btn-success btn-xs">Accept/Decline</a>
            </td>
          </tr>
          {% else %}
          <tr>
            <td>No Records found!</td>
          </tr>
          {% endfor %}
        </tbody>
    </div>
  </body>
</html>



      