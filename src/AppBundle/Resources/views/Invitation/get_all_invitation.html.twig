
<h3>Get All Sent Invitations</h3>
<table clas="table table-striped table-hover">
  <thead>
    <tr>
      <th>Sender Name</th>
      <th>Invited Name</th>
      <th>Message</th>
      <th>Status</th>
    </tr>
  </thead>
  <tbody>
    {% for post in posts %}
    <tr>
      <td>{{post.id}}</td>
      <td>{{post.senderName}}</td>
      <td>{{post.invitedName}}</td>
      <td>{{post.message}}</td>
      <td>{{post.status}}</td>
      <td>
        <a href="" class="btn btn-primary btn-xs">View</a>
        <a href="" class="btn btn-success btn-xs">Edit</a>
        <a href="" class="btn btn-danger btn-xs">Delete</a>
      </td>
    </tr>
    {% else %}
    <tr>
      <td>No Records found!</td>
    </tr>
    {% endfor %}
  </tbody>
</table>
      